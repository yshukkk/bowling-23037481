# Plan: 올원 게임 (All Ones)

## 목표
모든 굴림(roll)이 1핀인 게임(20번)의 점수는 20점이다.

## 추가할 테스트
- `test_game.py`: `test_all_ones_scores_20`
  - `Game()` 생성 후 `roll(1)`을 20번 호출
  - `score()`가 `20`을 반환하는지 검증

## 구현 범위
- 기존 프레임 단위 순회 `score()` 로직 그대로 사용 (스페어/스트라이크 없는 일반 프레임 합산이므로 별도 코드 변경이 필요 없을 가능성이 높음)
- 만약 이미 통과한다면, 회귀 테스트로서 커밋만 진행하고 `game.py` 변경 없음
- 스페어, 스트라이크, 10프레임 보너스 로직은 여전히 범위 밖 (다음 증분에서 다룸)
